const httpprotocol = require('http');

const localhost = "localhost"
const port = 3300;


    res.statusCode = 200;
    res.setHeader('Content-Type', 'text/plain');
    res.= ;

server.listen(port, protocol, (req,res) => {
    console.log(`Server is running. Please input http://${hostname}:${port}/`);
});

